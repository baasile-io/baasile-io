<% content_for(:back_link) { back_office_users_path } %>
<% content_for(:page_back) { back_office_users_path } %>
<% content_for(:page_title) { @page_title } %>

<div class="b-io-content--secondary">
  <div class="container-fluid">

    <ul class="nav nav-tabs" role="tablist">
      <li class="nav-item">
        <a class="nav-link active" href="#">Modifier</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= permissions_back_office_user_path(@user) %>">Permissions</a>
      </li>
    </ul>

    <%= render partial: 'form' %>

  </div>
</div>

<%= render partial: 'shared/versions', locals: {objects: [@user]} %>