<% content_for(:page_title) { t('misc.parameters') } %>

<div class="b-io-content--secondary">
  <div class="container-fluid">

    <%= render 'form' %>

  </div>
</div>


<%= render partial: 'shared/versions', locals: {objects: [@contract] + @contract.comments.to_a + [@contract.price]} %>