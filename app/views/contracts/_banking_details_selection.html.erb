<% content_for(:page_description) { t('.description') } %>
<% content_for(:page_title) { t('.title') } %>

<div class="b-io-content--secondary">
  <div class="container-fluid">

    <% if @banking_detail_templates.size == 0 %>
      <p class="text-muted"><%= t('misc.no_banking_detail') %></p>
      <%= link_to t('actions.create_banking_detail_template'), new_service_banking_detail_path(entreprise_owner, entreprise_owner_id: entreprise_owner, from_contract: current_contract.id, from_service: current_service.try(:id)), class: 'btn btn-primary' %>
    <% else %>
      <%
        @banking_detail_templates.each.with_index do |banking_detail_template, index|
          url = (!current_service.nil? ? select_banking_detail_service_contract_path(current_service, current_contract, banking_detail_id: banking_detail_template.id, entreprise_owner_id: entreprise_owner) : select_banking_detail_contract_path(current_contract, banking_detail_id: banking_detail_template.id, entreprise_owner_id: entreprise_owner))
      %>
        <% if index > 0 %>
        <hr>
        <% end %>
        <div class="row">
          <div class="col-md-3 text-md-right">
            <h4><%= banking_detail_template.name %></h4>
          </div>
          <div class="col-md-9">
            <%= render partial: 'banking_details/show_banking_detail_info', locals: {banking_detail: banking_detail_template} %>
            <hr />
            <div class="text-right">
              <%= link_to t('actions.select'), url, class: 'btn btn-primary btn-lg ' %>
            </div>
          </div>
        </div>
      <% end %>
    <% end %>

  </div>
</div>
