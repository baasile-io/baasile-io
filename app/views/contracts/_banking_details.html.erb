<% content_for(:page_title) { @contract.banking_details.find_by(service: entreprise_owner).name } %>



    <%= render partial: 'banking_details/show_banking_detail_info', locals: {banking_detail: @contract.banking_details.find_by(service: entreprise_owner)} %>

    <hr>
    <div class="row">
      <div class="col-4">
      </div>
      <div class="col-8">
        <%= link_to t('actions.edit'), (@service.nil? ? edit_contract_banking_detail_path(current_contract, id: @contract.banking_details.find_by(service: entreprise_owner).id, entreprise_owner_id: entreprise_owner) : edit_service_contract_banking_detail_path(current_service, current_contract, id: @contract.banking_details.find_by(service: entreprise_owner).id, entreprise_owner_id: entreprise_owner)), class: 'btn btn-secondary' %>
        <%= link_to t('actions.destroy'), (@service.nil? ? contract_banking_detail_path(current_contract, id: @contract.banking_details.find_by(service: entreprise_owner).id, entreprise_owner_id: entreprise_owner) : service_contract_banking_detail_path(current_service, current_contract, id: @contract.banking_details.find_by(service: entreprise_owner).id, entreprise_owner_id: entreprise_owner)), method: :delete, class: 'btn btn-danger' %>
        </div>
    </div>

