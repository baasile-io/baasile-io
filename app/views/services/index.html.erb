<% content_for(:page_title) { t('.title') } %>

<div class="b-io-content--secondary">
  <div class="container">
    <table class="table">
      <thead>
      <tr>
        <% if current_user.has_role?(:superadmin) %>
            <th>Admin</th>
        <% end %>
        <th>Name</th>
        <th>Activated</th>
        <th></th>
      </tr>
      </thead>
      <tbody>
      <% @collection.each do |service| %>
      <tr>
        <% if current_user.has_role?(:superadmin) %>
            <td>
              <%= link_to t('actions.admin_borad'), admin_board_service_path(service.id), class: 'btn btn-secondary' %>
            </td>
        <% end %>
        <td><%= service.name %></td>
        <td><%= service.is_activated? %></td>
        <td class="text-xs-right">
          <%= link_to t('actions.edit'), service_path(service.id), class: 'btn btn-secondary' %>
        </td>
      </tr>
      <% end %>
      </tbody>
    </table>
    <%= link_to t('actions.new'), new_service_path, class: 'btn btn-secondary' %><br />

  </div>
</div>
