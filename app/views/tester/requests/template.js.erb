$('#modal-window .modal-title').html('<%= escape_javascript(current_request.template_name) %>');
$('#modal-window .modal-body').html('<%= escape_javascript(render partial: "tester/requests/template") %>');

window.activate_select2();
window.activate_ace_editor();
window.activate_ace_viewer();