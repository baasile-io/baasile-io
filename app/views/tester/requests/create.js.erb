$('#modal-window .modal-title').html('<%= escape_javascript(t('tester.title')) %>');
$('#modal-window .modal-body').html('<%= escape_javascript(render partial: "show") %>');

window.activate_select2();
window.activate_ace_editor();