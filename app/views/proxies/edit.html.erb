<% content_for(:page_title) { t('.title') } %>

<div class="b-io-content--secondary">
  <div class="container-fluid">

    <%= render partial: 'form' %>

  </div>
</div>

<%= render partial: 'shared/versions', locals: {objects: [@proxy, @proxy.proxy_parameter, @proxy.proxy_parameter_test, @proxy.proxy_parameter.identifier, @proxy.proxy_parameter_test.identifier]} %>
