<%= I18n.t("mailer.contract_notifier.send_activate_contract_notification.#{@contract.activate ? "activate" : "disable" }.subject", name: @contract_name) %>

Lien vers le contrat : <%= contract_url(@contract, locale: @user_locale) %>
