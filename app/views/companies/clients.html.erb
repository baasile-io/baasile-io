<% content_for(:page_title) { t('.title') } %>
<% content_for(:page_controls) { link_to t('actions.new'), new_service_path(company_id: current_company.id, service_type: Service::SERVICE_TYPES_ENUM[:client]), class: 'btn btn-primary' } %>

<div class="b-io-content--secondary">
  <div class="container-fluid">

    <%= render partial: 'services_list', locals: {services: @collection, service_type: :client} %>

  </div>
</div>
