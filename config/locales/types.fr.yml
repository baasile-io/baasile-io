fr:
  types:
    authentication_modes:
      'null': 'Authentification non requise'
      oauth2: 'OAuth 2.0 client credentials'
    query_parameter_modes:
      mandatory: 'obligatoire'
      optional: 'optionnel'
      forbidden: 'interdit'
    protocols:
      http: 'HTTP'
      https: 'HTTPS'
    genders:
      male: 'male'
      female: 'female'
