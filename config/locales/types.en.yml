en:
  types:
    authentication_modes:
      'null': 'No authentication required'
      oauth2: 'OAuth2'
    modes:
      mandatory: 'mandatory'
      optional: 'optional'
      forbidden: 'forbidden'
    protocols:
      http: 'HTTP'
      https: 'HTTPS'
