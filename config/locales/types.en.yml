en:
  types:
    authentication_modes:
      'null': 'No authentication required'
      oauth2: 'OAuth2'
    query_parameter_modes:
      mandatory: 'mandatory'
      optional: 'optional'
      forbidden: 'forbidden'
    protocols:
      http: 'HTTP'
      https: 'HTTPS'
